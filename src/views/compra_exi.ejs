<%- layout('layout/main') %>

<div class="container py-3">

    <h3 class="my-2 text-center text-dark">Gracias por tu compra. ¡Te has inscrito a este curso sastifactoriamente!</h3>
    <p class="mb-3 text-dark text-center">Tu recibo también llegará a tu correo, es recomendable guardarlo para comprobar tu inscripción.</p>

    <div class="w-100 d-flex justify-content-center">
        <div class="card">
            <div class="card-body d-flex justify-content-center">
                <div>
                    <%- include("partials/_voucher") %>
    
                    <a href="/capacitaciones/todos/1" class="btn btn-primary btn-block my-3">Volver</a>
                </div>
            </div>
        </div>
    </div>

    <h4 class="text-info text-center mt-5 mb-4">Otro curso que te puede interesar</h4>
    <div class="w-100 d-flex justify-content-center mb-5">
        <div class="bg-primary" style="width: 200px; height: 2px;"></div>
    </div>
    <div class="d-flex justify-content-center flex-wrap">
        <% for(let i=0; i < courses.length; i++){ %>
            <div style="width: 340px;" class="card m-2 wow flipInY" data-wow-delay="0.2s">
                <a href="/payment/<%= courses[i].public_img_id %>"><img src="<%= courses[i].imageURL %>" height="261" class="card-img-top" alt="<%= courses[i].title %>"></a>
                
                <div class="card-body separar">
                    <a href="/payment/<%= courses[i].public_img_id %>"><h3 class="card-title text-primary"><%= courses[i].title %></h3></a>
    
                    <div class="datos-card">
                        <p class="my-1"><b style="color: #750099;"><i class="fa fa-bullhorn"></i> Modalidad:</b> <%= courses[i].modo %></p>

                        <p class="my-1"><b class="text-danger"><i class="fa fa-calendar-alt"></i> Fecha:</b> <%= moment(courses[i].day).format("DD/MM/YYYY") %></p>
                        <p class="my-1"><b class="text-success"><i class="fas fa-money-bill-wave"></i> Costo:</b>  <%= Intl.NumberFormat("de-DE").format(courses[i].price)%>Bs</p>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</div>