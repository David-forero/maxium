<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Administradores</title>
    <link rel="stylesheet" href="/css/bootstrap.min.cosmo.css">
    <style>
      body{background: #f3f3f3;}

      .formulario-login{
        margin-top: 5%;
      }
    </style>
  </head>
  <body>

    <div class="container">
      <div class="col-md-4 offset-md-4 formulario-login">
        <div class="card">
          <img src="/img/icon.png" alt="" class="card-img-top w-50 mx-auto">

          <form action="/nc-login" method="post" class="card-body" autocomplete="off">
            <hr>
            <div class="form-group">
              <input type="text" placeholder="Usuario" name="email" class="form-control py-3" autofocus/>
            </div>

            <div class="form-group">
              <input type="password" placeholder="Contraseña" name="password" class="form-control py-3"/>
            </div>

            <div class="form-group">
              <button class="btn btn-primary btn-block py-2 px-5">Entrar</button>
            </div>

            <% if(error.length > 0){ %>
              <div class="alert alert-danger text-center">
                  <%= error %>
              </div>
            <% } %>    
          </form>
        </div>
      </div>
    </div>
    
  </body>
</html>
