<%- layout('layout/dashboard') %>

<div class="col-lg-12 my-5 wow fadeInUp" data-wow-delay="0.2s">
    <h3 class="text-dark text-center">Permisos al panel.</h3>
    <p class="text-center">Acá se añadirá los ayudantes o moderadores donde podran administrar el sitio web añadiendo cursos o articulos.</p>
</div>

<div class="row">
    <div class="col-lg-4">
        <form method="POST" action="/nc-registro" autocomplete="off" class="card wow fadeInUp" data-wow-delay="0.5s">
            <div class="card-header">
                <h3 class="card-title">Añadir un rol <i class="fa fa-user-plus"></i></h3>
            </div>

            <div class="card-body">
                <div class="form-group">
                    <label>Nombre</label>
                    <input required type="text" class="form-control" name="name" autofocus>
                </div>

                <div class="form-group">
                    <label>Apellido</label>
                    <input required type="text" class="form-control" name="lastname">
                </div>

                <div class="form-group">
                    <label>Correo:</label>
                    <input required type="email" class="form-control" name="email" placeholder="example@company.com">
                </div>

                <div class="form-group">
                    <label>Contraseña:</label>
                    <input required type="password" name="password" class="form-control">
                </div>
                
                <div class="form-group">
                    <label>Confirmar contraseña:</label>
                    <input required type="password" name="passwordConfirm" class="form-control">
                </div>

                <div class="form-group">
                    <label>Selecciona un rol:</label>
                    <select required name="rol" class="form-control">
                        <option disabled selected>...</option>
                        <option value="mod">Moderador</option>
                        <option value="helper">Ayudante</option>
                    </select>
                </div>

                <div class="form-group">
                    <button class="btn btn-primary py-2 px-4 btn-block">Agregar</button>
                </div>
            </div>
        </form>
    </div>

    <div class="col-lg-8 wow fadeInUp" data-wow-delay="0.9s">
        <table class="table table-bordered table-hover">
            <thead class="bg-dark text-white">
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Correo</th>
                    <th>Rol</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                
            </tbody>
        </table>
    </div>
</div>